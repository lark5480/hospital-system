# ADR-006: 统一 IAM：自管 JWT，预留外部 IdP 接入

- **状态**: 已采纳
- **上下文**: 医院多角色（医/护/药/收/管）、需统一认证与等保审计。
- **决策**: 后端自管 JWT（`JwtTokenService` + `JwtAuthFilter`），七权分立 RBAC 已实现。默认态零配置可跑，未来接外部 IdP 只需替换 login 环节（校验外部 token → 换签自有 JWT）。
- **后果**: 易 — 零外部依赖、本地零配置可跑、七权分立可演示；难 — 自管 JWT 需自行处理令牌刷新、过期等场景。
